{
  "name": "cold-storage",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "COLD STORAGE — a first-person office stealth game about stealing the intern's lunch.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "play": "vite preview --port 4173 --strictPort --open"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
